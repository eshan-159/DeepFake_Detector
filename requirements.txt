# Lightweight requirements for Vercel deployment (demo mode)
# For full PyTorch inference, use requirements-full.txt locally

fastapi==0.110.1
python-multipart==0.0.9
pillow==10.3.0
numpy==1.26.4
pydantic>=2.0.0
